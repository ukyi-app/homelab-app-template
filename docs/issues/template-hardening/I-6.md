---
status: done
blocked-by: []
commit: d392883
---

> **Result**: `scaffold/common/renovate.json` 동봉 — 4개 아키타입 모두 앱 루트에 착지(gitignore 안 됨,
> 공식 validator repo-config 모드 통과).
>
> **핵심 발견**: 루트 설정을 그대로 복사했다면 **서류상만 맞고 실제로는 죽은 설정**이었다. Renovate의
> `bun` 매니저는 `updateLockedDependency`를 구현하지 않아(npm 매니저와 달리) 캐럿 범위 의존의
> 마이너·패치 — **CVE 픽스의 대부분** — 가 영구 정체한다. dry-run 실측: `hono: ^4.6.0`이 4.6.0에 lock된
> 상태에서 4.12.29가 범위 안에 있는데도 **브랜치 0개**. `rangeStrategy: bump` + `lockFileMaintenance`가
> package.json과 bun.lock을 함께 올려 `--frozen-lockfile` 빌드를 깨지 않는다.
> 부수 함정: `lockFileMaintenance`의 기본 schedule(`before 4am on monday`)은 최상위 schedule을
> **상속하지 않고**, homelab cron이 월 04:00 KST 정각이라 1분 차이로 영영 안 돈다 → 명시 핀.
>
> 앱은 템플릿과 다르다: `automerge: false` — 스캐폴드된 앱엔 **PR CI가 하나도 없어**(release.yaml은
> `push: main` 전용, template-ci는 자가삭제) Renovate가 체크 0개 브랜치를 green으로 보고, 머지 =
> GHCR push = autoDeploy = **프로덕션 배포**다. `github-actions` 비활성 — App 토큰에 `workflows:write`가
> 없고, 켜면 `reusable-app-build@main`을 SHA로 핀하려 들어 thin-caller SSOT를 깬다.
> 비자명한 선택은 Renovate의 `description` 필드로 파일 안에 기록(F-1 등록 시 `issues:write` 필요 —
> Dependency Dashboard가 이슈를 쓴다; homelab이 실제로 밟은 함정).

# I-6 — Renovate 설정 동봉

## Parent

`docs/prds/template-hardening.md`

## What to build

스캐폴드된 앱이 Renovate 설정을 상속해, **homelab 쪽 레포 등록만 되면 즉시**
base 이미지·의존성 보안 패치가 흐르기 시작할 준비를 갖추게 한다. 현재 renovate
설정은 템플릿 루트에만 있어 생성된 앱에 복사되지 않으며, 그 결과 생성된 앱의
base 이미지·의존성 핀이 영구히 정체된다.

공통 스캐폴드 자산에 renovate 설정을 추가한다 — 템플릿 루트 설정과 같은 정책
(권장 프리셋 + digest 핀)을 따른다.

**범위 경계**: homelab 쪽 레포 등록 배선(Renovate 실행 대상 목록 추가 또는
allowlist autodiscover 전환)은 교차-레포 작업으로 이 배치의 범위 밖이며,
PRD의 Follow-up Backlog F-1로 추적된다. 이 이슈는 "등록 즉시 동작할 준비"까지만
책임진다.

## Acceptance criteria

- [ ] 4개 아키타입 어느 것으로 스캐폴드해도 앱 루트에 renovate 설정이 존재한다.
- [ ] 설정이 템플릿 루트와 동일한 정책(권장 프리셋 + digest 핀)을 따른다.
- [ ] 스캐폴더가 남기는 파일 목록에 renovate 설정이 포함되고, 스캐폴드 자체는
      계속 성공한다(자가삭제·lock 재생성 경로 영향 없음).

## Blocked by

None - can start immediately.
