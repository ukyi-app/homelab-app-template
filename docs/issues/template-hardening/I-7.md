---
status: done
blocked-by: [I-1, I-2, I-5]
commit: 266a784
---

> **Result**: 4개 아키타입 스모크 런 + 액션 SHA 핀 + bun 정합 가드. 모든 가드가 무는 것을
> ubuntu-24.04-arm 컨테이너에서 YAML의 `run:` 블록 원문으로 실증(네거티브 컨트롤 전량 FAIL 확인).
>
> **리뷰가 뒤집은 것들**:
> - **프로브 경로 하드코딩은 스모크의 존재 이유를 놓친다** — scaffold가 `/livez`로 드리프트하면
>   스키마 검증(경로 *값*은 안 봄)도 하드코딩된 스모크도 초록인데 차트만 404 → CrashLoop.
>   생성된 `.app-config.yml`에서 **유도**하도록 수정(드리프트 시 FAIL, 양쪽이 함께 바뀌면 PASS로 실증).
> - **상태코드만 보는 프로브는 무용지물** — fullstack의 SPA catch-all이 `/healthz`를 지워도
>   `index.html`을 200으로 준다. 본문까지 단언해야 잡힌다. site도 SWS 베이스가 **자체 index.html을
>   서빙**하므로 `/ → 200`은 반증 불가였다 → 앱의 `<title>` 확인으로 교체.
> - **I-6 리뷰가 넘긴 전제가 틀렸다** — Renovate의 github-actions 매니저는 `bun-version:`을 이미
>   파싱한다. 그래서 customManager를 얹으면 중복 dep이 생겨 bun이 **두 브랜치로 쪼개지고**
>   (CI만 범프하는 PR = 가드 레드), 게다가 워크플로를 그룹에 묶으면 `workflows:write` 부재로
>   **PR이 아예 안 열린다**. 해법: bun SSOT를 `.bun-version`(Renovate가 쓸 수 있는 경로)으로 옮기고
>   setup-bun이 `bun-version-file`로 읽게 했다 → 4개 Dockerfile + `.bun-version`이 **한 브랜치**로
>   함께 범프(dry-run 실증). 내장 `bun-version` 매니저는 npm datasource로 같은 중복을 재생성하므로 비활성.
> - worker 스모크의 판별 마진이 얇았다(회귀 3.45s vs 상한 3s) → 불필요한 `sleep 1` 제거 + 상한 2s
>   (정상 0.1s / 회귀 4.6s). `INTERVAL_MS`를 소스에서 읽어 단언 — 주기를 낮춰 가드를 defang하는 걸 막는다.
> - `.bun-version`이 생성 앱으로 새어 나가 앱마다 무의미한 Renovate PR을 여는 것을 발견 → 템플릿 전용
>   머신러리로 분류해 스캐폴드가 삭제한다.

# I-7 — template-ci 강화: 스모크 런·SHA 핀·bun 버전 정합

## Parent

`docs/prds/template-hardening.md`

## What to build

템플릿 드리프트 가드가 "빌드는 통과하나 기동은 실패"하는 회귀를 잡게 만든다.
현재 template-ci는 4개 아키타입 이미지를 build만 하고 **한 번도 기동하지 않아**,
빌더/런타임 베이스의 glibc 갭이나 엔트리포인트·경로·포트 어긋남 같은 런타임 파손이
CI green으로 통과한다.

- **스모크 런(4개 아키타입 전부)**: 빌드된 이미지를 실제로 기동해 런타임 계약을
  검증한다.
  - web/api: liveness·readiness 경로가 200을 반환한다(api는 무DB 시 readiness가
    정적 통과하므로 부가 인프라가 필요 없다).
  - worker: 기동 로그 확인 + **sleep 도중 SIGTERM을 보내** 제한 시간 내(수 초)
    종료·종료 코드 0·종료 로그를 단언한다(I-1의 중단 가능 sleep 직접 검증).
  - site: 최소 인자(포트·루트·헬스)만 주입해 헬스 경로를 확인한다 — 차트의 세부
    인자 재현은 하지 않는다.
- **액션 SHA 핀**: template-ci가 서드파티 GitHub 액션을 이동 태그로 참조한다
  (homelab은 전부 커밋 SHA 핀). 이 워크플로는 포크 PR에서도 실행되므로 태그 이동/
  탈취가 공급망 표면이 된다. 커밋 SHA(+버전 주석)로 핀한다.
- **bun 버전 정합 grep 가드**: bun 버전이 4개 Dockerfile과 CI 설정 등 여러 곳에
  하드코딩되어 있어, 부분 범프가 "lock 재생성 bun ≠ frozen-lockfile 빌드 bun"을
  조용히 만들 수 있다. 모든 지점의 버전이 일치하는지 검증하는 스텝을 추가한다
  (불일치 시 실패).
- **DX 존재 계약**: 스캐폴드된 fullstack에 dev 스크립트와 concurrently
  devDependency가 존재함을 단언한다(기존 계약-검증 스타일). `bun run dev`를 실제로
  기동하는 프로세스 seam은 두지 않는다(PRD P-5 결정 — 플레이크 위험).

## Acceptance criteria

- [ ] 4개 아키타입 각각에 대해 빌드된 이미지를 기동하는 스모크 스텝이 있고, CI가
      green이다.
- [ ] web/api 스모크가 liveness·readiness 200을 확인한다.
- [ ] worker 스모크가 sleep 도중 SIGTERM에 대해 제한 시간 내 종료·코드 0·종료
      로그를 단언한다(I-1 이전 코드였다면 실패했을 검증).
- [ ] site 스모크가 최소 인자로 기동해 헬스 경로를 확인한다.
- [ ] template-ci의 서드파티 액션이 모두 커밋 SHA로 핀되어 있다.
- [ ] bun 버전이 어느 한 곳이라도 불일치하면 CI가 실패한다(의도적 불일치로 확인).
- [ ] 스캐폴드된 fullstack에 dev 스크립트·concurrently devDep이 있음을 단언한다.
- [ ] 기존 검증(계약 스키마, 네거티브, 풀러-안전 가드, 타입검사)이 모두 유지된다.

## I-6 리뷰가 넘긴 제약 (이 이슈에서 반드시 고려)

1. **bun 정합 가드 vs Renovate** — 4개 Dockerfile의 bun 태그가 동일하므로 Renovate는 이미 **한 PR로
   묶어** 올린다(`groupName` 불필요). 진짜 문제는 `template-ci.yaml`의 `bun-version:`이 **setup-bun의
   입력값**이라 Renovate의 github-actions 매니저가 **파싱하지 않는다**는 것 — Dockerfile만 범프되고 CI
   값은 남아 **이 이슈가 추가하는 가드가 매번 레드**가 된다. 루트 `renovate.json`에 `customManagers`
   정규식(+ `oven/bun`과 동일 `groupName`)을 넣어 두 지점이 함께 움직이게 해야 가드가 실용적이다.
2. **액션 SHA 핀 vs 토큰 권한** — PRD는 "SHA 핀 + Renovate가 SHA 갱신"을 상정했으나, homelab의
   Renovate App 토큰에는 `workflows:write`가 **없다**(homelab 워크플로 주석에 기록). 즉 Renovate는
   `.github/workflows/*`를 push하지 못해 **SHA 핀을 갱신할 수 없다**. SHA 핀 자체는 보안상 옳으므로
   유지하되, 갱신이 수동임을 명시하고 토큰 스코프 확장은 follow-up으로 남긴다.

## Blocked by

- I-1 (worker SIGTERM 종료 동작이 존재해야 스모크가 통과)
- I-2 (Dockerfile 게이트·동봉 테스트가 존재해야 함)
- I-5 (fullstack dev 스크립트·concurrently가 존재해야 단언 가능)
