{
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build --compile --target=$BUN_COMPILE_TARGET src/index.ts --outfile app",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  }
}
